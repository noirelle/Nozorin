version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "3001:3001"
    environment:
      - PORT=3001

  ui:
    build: ./ui
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
